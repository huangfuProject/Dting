<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dting.show.server.mapper.MessageNetworkDataMapper">

    <insert id="ignoreOnlyBatchInsert">
        insert ignore into
        message_network_data(`unique`,`message_tag`,`message_ip`,`collect_time`,`network_data`)
        values
        <foreach collection="messageNetworkDataList" item="messageNetworkData" separator=",">
            (#{messageNetworkData.unique}, #{messageNetworkData.messageTag}, #{messageNetworkData.messageIp},
            #{messageNetworkData.collectTime}, #{messageNetworkData.networkData})
        </foreach>
    </insert>
</mapper>